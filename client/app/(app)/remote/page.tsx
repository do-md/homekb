"use client";

import { RemoteView } from "@/features/kb/components/views/remote";

/** /remote — device-connection hub (pairing on desktop, connection card on web). */
export default function RemotePage() {
  return <RemoteView />;
}
