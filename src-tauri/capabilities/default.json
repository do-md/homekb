{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window default capabilities: core only (custom invoke commands require no extra plugin permissions)",
  "windows": ["main"],
  "permissions": ["core:default"]
}
