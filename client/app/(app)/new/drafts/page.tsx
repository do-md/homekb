"use client";

import { DraftsView } from "@/features/kb/components/views/drafts";

/** /new/drafts — the drafts collection (focused mode under the New note tab). */
export default function DraftsPage() {
  return <DraftsView />;
}
