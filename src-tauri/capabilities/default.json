{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window default capabilities: core + self-update (updater check/install, process relaunch)",
  "windows": ["main"],
  "permissions": ["core:default", "updater:default", "process:default"]
}
